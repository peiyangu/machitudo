# 店舗データ管理システム - 使い方

## 📁 ファイル構成

- `assets/stores-data.js` - 全店舗データ（250店舗分）
- `assets/stores-render.js` - レンダリングロジック
- 各ジャンルページ（sweets.html, drink.html など）

## ✏️ 店舗データの追加方法

### 1. stores-data.jsを開く

### 2. allStoresData配列に店舗情報を追加

```javascript
{
  name: "店舗名",
  description: "店舗の説明文（50-100文字程度）",
  image: "assets/images/stores/画像ファイル名.jpg",
  instagram: "https://www.instagram.com/アカウント名/",
  days: ["1/31", "2/1"],  // 両日参加の場合
  // days: ["1/31"],      // 1/31のみの場合
  // days: ["2/1"],       // 2/1のみの場合
  genre: "sweets"  // sweets, drink, food, cafe, handmade, workshop, festival
}
```

### 3. ジャンルの種類

- `sweets` - スイーツ
- `drink` - ドリンク
- `food` - 飲食
- `cafe` - カフェ
- `handmade` - ハンドメイド
- `workshop` - ワークショップ
- `festival` - フェスティバル

### 4. 参加日の指定

- `["1/31", "2/1"]` - 両日参加（ゴールドのバッジ）
- `["1/31"]` - 1/31のみ（薄いゴールドのバッジ）
- `["2/1"]` - 2/1のみ（薄いゴールドのバッジ）

## 🖼️ 画像の準備

### 画像の配置場所
`assets/images/stores/` フォルダに画像を配置

### 推奨仕様
- 形式: JPG または PNG
- サイズ: 横800px × 縦600px 程度
- ファイルサイズ: 200KB以下（圧縮推奨）
- 命名規則: `ジャンル名_店舗名.jpg`（例: `sweets_cakeshop1.jpg`）

### 画像がない場合
- `onerror`属性により自動的に `no-image.jpg` が表示されます
- `assets/images/stores/no-image.jpg` を用意しておいてください

## 📝 CSVからの一括変換（オプション）

### 1. Excelまたはスプレッドシートで店舗リストを作成

| 店舗名 | 説明 | 画像ファイル名 | Instagram | 参加日 | ジャンル |
|--------|------|---------------|-----------|--------|----------|
| ○○カフェ | 美味しいコーヒー | cafe1.jpg | @cafe_example | 両日 | cafe |

### 2. CSVとして保存

### 3. 以下のJavaScriptで変換

```javascript
// CSVの内容をコピー＆ペーストして実行
const csvData = `店舗名,説明,画像ファイル名,Instagram,参加日,ジャンル
○○カフェ,美味しいコーヒー,cafe1.jpg,@cafe_example,両日,cafe`;

const lines = csvData.split('\n').slice(1); // ヘッダーをスキップ
const stores = lines.map(line => {
  const [name, desc, image, ig, days, genre] = line.split(',');
  const daysArray = days === '両日' ? ['1/31', '2/1'] : 
                    days === '1/31のみ' ? ['1/31'] : ['2/1'];
  return {
    name: name.trim(),
    description: desc.trim(),
    image: `assets/images/stores/${image.trim()}`,
    instagram: `https://www.instagram.com/${ig.trim().replace('@', '')}/`,
    days: daysArray,
    genre: genre.trim()
  };
});

console.log(JSON.stringify(stores, null, 2));
// 出力されたJSONをコピーしてstores-data.jsに貼り付け
```

## 🎨 表示のカスタマイズ

### 店舗数を変更
`stores-render.js` の54行目あたり:
```javascript
function updateStoreCount(count, genre) {
  const countElement = document.getElementById('storeCount');
  if (countElement) {
    countElement.textContent = `${genreNames[genre]}：全${count}店舗`;
  }
}
```

### カードのデザイン変更
`assets/style.css` の `.store-card` セクションを編集

## 🐛 トラブルシューティング

### 店舗が表示されない
1. ブラウザの開発者ツール（F12）でコンソールエラーを確認
2. `stores-data.js` の構文エラーをチェック（カンマの抜けなど）
3. ジャンル名が正しいか確認（sweets, drink, food, cafe, handmade, workshop, festival）

### 画像が表示されない
1. 画像ファイルのパスが正しいか確認
2. 画像ファイルが実際に存在するか確認
3. ファイル名の大文字小文字に注意（Macでは動くがLinuxサーバーで動かない可能性）

### 参加日バッジが表示されない
1. `days` 配列の形式が正しいか確認: `["1/31"]` または `["2/1"]` または `["1/31", "2/1"]`
2. CSS が正しく読み込まれているか確認

## 💡 ヒント

- 250店舗分のデータ入力は、スプレッドシート → CSV → JavaScript変換がおすすめ
- 各店舗の説明文は50-100文字が見やすい
- 画像は事前に最適化（圧縮）しておくとページの読み込みが速くなります
- Instagram URLがない場合は、公式アカウントのURLを入れるか、空文字 `""` でOK
